#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first number.
 * @b: second number.
 * Return: the product of first number and second number.
 */
int mul(int a, int b)

{
	return (a * b);
}
